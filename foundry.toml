[profile.default]
src = "contracts"
out = "out"
libs = ["node_modules", "lib"]
solc = "0.8.20"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
tempo = "${TEMPO_RPC_URL}"

[etherscan]
tempo = { key = "", url = "https://explorer.tempo.xyz/api" }
